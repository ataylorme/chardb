SPECIFICATION Spec

CONSTANTS
    Shards     = {"s1", "s2"}
    BarrierIds = {"b1", "b2"}

INVARIANTS
    TypeOK
    BarrierMonotone
    NoMissingAcks
    BookmarkSurvivesWrites

PROPERTIES
    EventuallyComplete

\* Bound the model: cap each shard's op-log so the state space stays finite.
CONSTRAINT OpLogBound

